# Global `ctags` file that is smylinked to `$HOME/.ctags`

# Basic options
--recurse=yes
--tag-relative=yes
--exclude=.git
--exlcude=tags

# Markdown syntax
--langdef=markdown
--langmap=markdown:.md
--regex-markdown=/^# ([a-zA-Z0-9]+)/\1/

