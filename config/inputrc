# Test to see if comments are supported in `.inputrc` file
# NOTE: alacritty currently does not support bracketed-paste
set enable-bracketed-paste on

# Make Tab autocomplete
set completion-ignore-case on

# List all matches in case possible completions are possible
set show-all-if-ambiguous on

# Flip through autocompletion matches with`shift+tab`
"\e[Z": menu-complete

# Filtered history search
"\e[A": history-search-backward
"\e[B": history-search-forward

# Credit: https://medium.com/@webprolific/getting-started-with-dotfiles-43c3602fd789
