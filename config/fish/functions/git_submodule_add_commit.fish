function git_submodule_add_commit --description 'git add & commit within submodules'
  # TODO: get a working function working.
  # git submodule  foreach --recursive  |  tail  -r | sed 's/Entering//' | xargs -I% cd % ; git add -A and git commit
end