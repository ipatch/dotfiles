function fish_term_wrap
  # if [ $setterm -linewrap = 'off' ]
  #   # echo (prompt_pwd) $COLUMNS x $LINES
  #   setterm -linewrap = 'on'
  # else
  #   # echo $_
  #   setterm -linewrap = 'off'
  # end

  echo "Currently under construction 🚧"
end
