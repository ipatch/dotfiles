function .... --description "go up two directories, ie. cd ../.." 

  #NOTE: this function could be much cooler ™️ if it could take additional dots as ars,
  #...ie, ...... six dots would go up 3 levels, and just have the func called `..` dot dot
  cd ../..

end
